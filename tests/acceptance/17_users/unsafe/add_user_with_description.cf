#######################################################
#
# Test adding a user with a description.
#
#######################################################

body common control
{
      inputs => { "../../default.cf.sub" };
      bundlesequence  => { default("$(this.promise_filename)") };
}

#######################################################

bundle agent init
{
  # Remove him first, should he already be present.
  users:
    "johndoe"
      policy => "absent";
}

#######################################################

bundle agent test
{
  vars:
    "desc_string" string => "This description should make the CFEngine test pass";

  users:
    "johndoe"
      policy => "present",
      description => "$(desc_string)";
}

#######################################################

bundle agent check
{
  classes:
    "ok" not => strcmp("0", countlinesmatching("johndoe:[^:]*:[^:]*:[^:]*:$(test.desc_string):.*", "/etc/passwd"));

  reports:
    ok::
      "$(this.promise_filename) Pass";
    !ok::
      "$(this.promise_filename) FAIL";
}
